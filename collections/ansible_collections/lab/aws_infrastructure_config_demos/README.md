# lab.aws_infrastructure_config_demos (vendored)

This collection is vendored inside the ansible/product-demos repository. It includes AWS infrastructure demo roles from the former `aws.infrastructure_config_demos` collection and VPC HTML report roles from `aws_demos`.

See `LICENSE` and `LICENSE.aws_demos` for license text.
